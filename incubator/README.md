# Incubator

This directory contains inactive application material preserved for possible
future reuse. Nothing under `incubator/` is included by the active homelab
deployment, validation, reconciliation, backup, or systemd setup.

The bundles retain the original Quadlet definitions and application-specific
integration notes. They are not deployable as-is; re-integrate and validate a
bundle before using it again.
