services:
  searxng-core:
    image: docker.io/searxng/searxng:2026.5.29-780ee3256 # https://hub.docker.com/r/searxng/searxng/tags
    container_name: searxng-core
    restart: always
    networks:
      - searxng
    volumes:
      - ${DATA_PATH}/searxng/configs:/etc/searxng
      - ${DATA_PATH}/searxng/volumes/data/data:/var/cache/searxng
    depends_on:
      - searxng-valkey

  searxng-valkey:
    container_name: searxng-valkey
    image: docker.io/valkey/valkey:9.1.0 # https://hub.docker.com/r/valkey/valkey
    volumes:
      - ${DATA_PATH}/searxng/volumes/valkey:/data
    restart: always
    command: valkey-server --save 30 1 --loglevel warning
    networks:
      - searxng

networks:
  searxng:
    external: true